//上传maven
afterEvaluate {
    publishing { // 发布配置
        publications { // 发布的内容
            release(MavenPublication) {
                from(components["release"])

                groupId = 'com.github.godlikewangjun'
                artifactId = 'autosize'
                version = '2.1.0'
            }
        }
    }
}


